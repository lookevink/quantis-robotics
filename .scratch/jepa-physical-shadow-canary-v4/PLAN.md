# Paused unknown-start physical shadow canary v2

Supersede the terminal v1 apparatus negative with a new identity. Before any
claim, stop Isaac playback and reauthenticate every reset-v5 state and visual
variant dimension through the read-only binder. Only then claim one fresh
512×512 RGB observation, model response, shadow-only search, and
counterfactual safety evaluation. Apply zero actions; do not train or film.

The v1 run ended before capture because the process timeline had resumed. It
is not model evidence and must never be reused or overwritten.
