# Continuity-safe unknown-start physical shadow canary v3

Bind one new zero-actuation canary to the recovery-verified reset-v6 recording
`unknown-start-reset-v6-62605`. Before claiming the experiment, pause Isaac and
independently authenticate the complete live state against the reset result.
Then capture one 512x512 wrist observation, evaluate the unchanged frozen JEPA
world model, bounded residual router, planner gates, and counterfactual Isaac
safety projection. Apply zero actions and do not train or film.

Reset v6 moved evidence capture to the paused state, correcting the lifecycle
discontinuity that invalidated reset-v5 handoff attempts. This experiment does
not alter the model, representation, residual artifact, planner, controller,
or safety thresholds. A valid representation/model gate rejection is terminal;
known code, deployment, simulator, and AWS defects are repaired autonomously.
