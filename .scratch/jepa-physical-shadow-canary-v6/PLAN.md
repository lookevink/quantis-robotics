# Paused-render unknown-start physical shadow canary v4

Repeat the frozen zero-actuation reset-v6 model gate under a new terminal
identity after the preceding canary proved a paused-camera apparatus defect
before producing an image or invoking the model. Keep reset seed `62605`,
planner seed `237`, all model artifacts, routing logic, thresholds, and safety
gates unchanged.

The only runtime correction renders newly attached cameras through
Replicator's paused, zero-delta path and verifies safety before and after every
render. A non-claiming repro produced complete RGB frames while leaving the
timeline paused, and the full reset state reauthenticated afterward. This run
applies zero actions and performs no training or filming.
