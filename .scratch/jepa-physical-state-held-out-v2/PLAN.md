# Physical-state residual disjoint held-out gate v2

## Terminal result

Evaluate the unchanged authenticated physical-state residual artifact exactly
once on the same two sealed canonical HELD_OUT recordings, after proving the
repaired runtime and the consumed v1 no-score result. Produce one immutable
terminal report and stop. Do not train, run Isaac, issue a JEPA action, or film.

## Frozen correction

The v1 experiment consumed canonical access before authenticating the complete
JEPA-WM/DINOv3 runtime. V2 must authenticate the runtime, base checkpoint,
source revisions, residual artifact, training contract, TRAIN evidence,
adjudication, v1 access claim and v1 terminal failure before creating its own
exclusive access claim. It must also prove that v1 produced no evaluation
report. Only canonical recording authentication and scoring may follow the v2
claim.

The model artifact, controller-independent scoring protocol, roster, windows,
labels, router, residual bounds and numerical gates are unchanged from v1.

## Stopping rule

Pre-claim failures may be corrected without canonical access. Once the v2 claim
exists, process loss or any exception is terminal and there is no retry in this
milestone. A pass is disjoint offline evidence only. A failure is one preserved
negative. Neither authorizes retraining, Isaac, live control, filming, hardware,
or production.
