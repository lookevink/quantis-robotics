# Milestone 20 unknown-start live action v3

Repeat the v2 one-action contract under a fresh immutable identity. Preserve
every reset, source, model, controller, safety, tracking, and recovery
fingerprint and threshold. Change only the harness cadence: perform candidate
capture, source-response binding, final handoff reauthentication, and the
single apply call inside one exclusive in-process Isaac operation.

Apply at most the first bounded source action, pause afterward, and require a
byte-identical 16 GiB recovery copy before terminal success. Do not weaken the
freshness gate, train, film, attach new authority, or continue automatically to
a second action.
