# Milestone 20 unknown-start live action v4

Repeat the v3 one-action contract under a fresh immutable identity. Preserve
every reset, source, model, controller, safety, tracking, time limit, and
recovery fingerprint and threshold. Change only the candidate policy routing:
after synchronized live continuity passes, authorize its bound observation
and response with the existing `InsertionEvaluationRefresh` mechanism already
used by insertion trials and direct contact-grasp execution.

Apply at most the first bounded source action, pause afterward, and require a
byte-identical 16 GiB recovery copy before terminal success. Do not weaken a
freshness gate, train, film, attach new authority, or continue automatically to
a second action.
