# Milestone 20 unknown-start reset contract v1 result

## Frozen outcome

The reset-only prerequisite is frozen with contract fingerprint
`bd745c96700f544aa93cbf1262a3bbde4bb8d550ddebc21a5c89917e762a13aa`.
It binds the exact exploration sampler source fingerprint
`0ec746dbf12fbed61c66b3c64dee6717fa15f015be4aad23e0f40e5b47a5228d`,
reserved held-out seed namespace `62600..62699`, perturbation distribution,
absolute connector/socket/end-effector workspace, contact-insertion socket
scale, and every sampled arm/camera/scene/light realization under explicit
tolerances.

Valid evidence requires one reset-time state set, zero replayed prefix frames,
zero applied actions, unattached/collision-free state, and zero initial contact
force. The contract grants reset authentication only; runtime motion remains
drive-only but no motion is authorized by this result.

Both independent standards and specification reviews passed. The full
authenticated remote suite passed all 831 tests. Isaac remained stopped. No
seed was exclusively claimed, no live reset was drawn, and no capture, model
evaluation, action, training, or filming occurred.

## Boundary

The next experiment must separately claim one unused reserved seed and may use
it once for zero-actuation reset/observation authentication. A newly observed
failure class ends that experiment; it must not be repaired and retried inside
the same live milestone. End-to-end motion and filming remain closed.
