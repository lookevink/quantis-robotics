# Milestone 20 unknown-start reset authentication v2

Run one reset-only authentication under the unchanged frozen contract using
recording identity `unknown-start-reset-v2-62601` and unused reserved seed
`62601`. Preserve the terminal v1 seed-62600 negative without modification.

The only implementation correction is to evaluate authored light-exposure
readback with the contract's frozen `1e-6` realization tolerance instead of
the reset adapter's accidental stricter `1e-9` duplicate. No physics, force,
tracking, workspace, attachment, action, training, or filming limit changes.

Known implementation, harness, controller, recording, and AWS defects may be
diagnosed and corrected autonomously. They do not require a user checkpoint.
Pause for the user only if reproducible evidence proves the representation or
model is insufficient for the task. This reset gate still applies zero actions
and earns no motion, training, or filming authority by itself.
