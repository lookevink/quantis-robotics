# Milestone 20 unknown-start reset authentication v3

Run the same frozen reset-only contract with recording identity
`unknown-start-reset-v3-62602` and unused seed `62602`. Preserve the terminal
v1/v2 ledgers, negatives, and seeds.

V3 authenticates the sampled arm initialization against the authored drive
target read back from USD, while retaining settled physical joints as separate
observed evidence. It authenticates the workspace against the synchronized
gripper control-frame position rather than the Panda hand-link origin. No
distribution, tolerance, physics, force, collision, attachment, action,
training, or filming gate changes.

Known implementation defects continue to be repaired autonomously. Pause for
the user only if reproducible evidence proves a representation or model
insufficiency.
