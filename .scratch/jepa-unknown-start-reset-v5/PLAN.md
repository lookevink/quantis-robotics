# Milestone 20 unknown-start reset authentication v5

Run the reset-only contract with recording identity
`unknown-start-reset-v5-62604` and unused held-out seed `62604`. Preserve all
terminal v1-v4 ledgers, negatives, recovery copies, and spent seeds.

V5 advances the contract to `quantis.unknown_start_reset_contract.v3` and
fingerprints that the existing `1e-5 m` position-realization tolerance also
applies when checking observed workspace containment. This corrects float32
physics readback without enlarging the sampled workspace distribution. Values
outside that existing tolerance still fail.

No action, model, training, filming, physics, force, collision, attachment,
camera, scene, or coordinate-frame authority changes. Known implementation
defects continue to be repaired autonomously; pause only for reproducible
representation or model insufficiency.
